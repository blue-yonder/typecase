sudo: false
language: python
python:
- "2.7"
- "3.4"
- "3.5"
install: pip install tox-travis python-coveralls
script: tox
after_success:
- if [[ "$COVERAGE" == "true" ]]; then coveralls || echo "failed"; fi
